% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_ilp_display.R
\name{mod_ilp_display_ui}
\alias{mod_ilp_display_ui}
\title{ILP Display UI}
\usage{
mod_ilp_display_ui(id, title = NULL, sections = c("prev_coach", "compare"))
}
\arguments{
\item{id}{Character. Module namespace id.}

\item{title}{Character or NULL. Optional header rendered above the panel.}

\item{sections}{Character vector of sections to render, in order. Defaults
to \code{c("prev_coach", "compare")}. Allowed values:
\itemize{
\item \code{"prev_coach"} — prior coach ILP summary text (\code{coach_mile_goal},
\code{coach_ilp_final} from the previous-period \code{coach_rev}).
\item \code{"compare"} — per-domain side-by-side: previous goal + achievement +
reflection on the left, current goal on the right (3 domain rows).
\item \code{"prev_goals"} / \code{"current"} — legacy stacked layout (kept for
back-compat; not in the default).
}}
}
\value{
A \code{shiny::tagList} with one \code{uiOutput()} per requested section.
}
\description{
Read-only display of a resident's ILP across two periods, structured for
coach review. Renders (in order): previous-period coach ILP summary,
previous-period goals with the resident's achievement reflection, and the
current-period goals the resident has set.
}
\details{
Coach-entry textareas (\code{coach_mile_goal}, \code{coach_ilp_final}) are NOT part
of this module — the consumer adds them around \code{mod_ilp_display_ui()}.
}
\seealso{
\code{\link[=mod_ilp_display_server]{mod_ilp_display_server()}}

Other ilp_display: 
\code{\link{mod_ilp_display_server}()}
}
\concept{ilp_display}
