% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_seval_reflection_display.R
\name{mod_seval_reflection_display_server}
\alias{mod_seval_reflection_display_server}
\title{Self-Eval Reflection Display Server}
\usage{
mod_seval_reflection_display_server(
  id,
  current_row,
  previous_row = NULL,
  fields = c("plus", "delta", "well", "discussion", "prog_assist")
)
}
\arguments{
\item{id}{Character. Module namespace id (must match the UI call).}

\item{current_row}{A reactive returning a 1-row data frame (or NULL/empty)
containing any of \code{s_e_plus}, \code{s_e_delta}, \code{s_e_well}, \code{s_e_discussion},
\code{s_e_prog_assist}. Typically the resident's \code{s_eval} row for the period
being reviewed.}

\item{previous_row}{Optional reactive returning a 1-row data frame for
the previous period. Pass NULL to suppress the prior-period reference panel.}

\item{fields}{Character vector matching the UI call (so the server only
wires up the outputs the UI actually rendered).}
}
\value{
Invisibly, NULL.
}
\description{
Server-side renderer for \code{\link[=mod_seval_reflection_display_ui]{mod_seval_reflection_display_ui()}}. Pulls the
relevant \verb{s_e_*} columns from the supplied current-period row and, if
provided, surfaces the previous-period text in a styled reference panel
above each block.
}
\details{
Mirrors ind.dash's gating for optional fields: \code{s_e_discussion} and
\code{s_e_prog_assist} are completely suppressed when both periods are empty
(rather than showing a placeholder). \code{s_e_plus} / \code{s_e_delta} / \code{s_e_well}
always render — they show an italic placeholder when the resident hasn't
answered yet.
}
\seealso{
\code{\link[=mod_seval_reflection_display_ui]{mod_seval_reflection_display_ui()}}

Other seval_display: 
\code{\link{mod_seval_boards_display_server}()},
\code{\link{mod_seval_boards_display_ui}()},
\code{\link{mod_seval_reflection_display_ui}()}
}
\concept{seval_display}
