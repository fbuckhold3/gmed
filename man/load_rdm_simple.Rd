% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_rdm_simple.R
\name{load_rdm_simple}
\alias{load_rdm_simple}
\title{Simple RDM 2.0 Data Loader for Shiny Apps}
\usage{
load_rdm_simple(
  rdm_token = NULL,
  redcap_url = "https://redcapsurvey.slu.edu/api/",
  for_app = "general",
  verbose = TRUE,
  config_fallback = TRUE
)
}
\arguments{
\item{rdm_token}{Character. RDM REDCap token. If NULL, tries environment variable RDM_TOKEN}

\item{redcap_url}{Character. REDCap API URL (default: SLU)}

\item{for_app}{Character. App type hint for optimizations ("individual", "coaching", "ccc", "general")}

\item{verbose}{Logical. Print loading progress (default: TRUE)}

\item{config_fallback}{Logical. Try config.yml if environment variables missing (default: TRUE)}
}
\value{
List with standardized structure:
\itemize{
\item residents: Clean resident data with levels
\item assessment_data: All assessment records properly formatted for gmed modules
\item all_forms: All form data organized by name
\item historical_medians: Milestone medians from complete historical data
\item milestone_data: Processed milestone data for visualizations
\item data_dict: RDM data dictionary
\item config: Configuration used (for debugging)
}
}
\description{
One-stop function to load all RDM 2.0 data with sensible defaults.
Designed to replace complex app-specific data loading with a single function call.
}
\details{
This function handles:
\itemize{
\item Token configuration (environment vs config file)
\item Complete data loading with proper filtering
\item Level-at-time calculations
\item Historical milestone medians
\item Ready-to-use data structure for gmed modules
}
}
\examples{
\dontrun{
# Simple usage - auto-detects token
app_data <- load_rdm_simple()

# With explicit token
app_data <- load_rdm_simple(rdm_token = "your_token_here")

# For specific app type
app_data <- load_rdm_simple(for_app = "individual")

# Then use in your Shiny app:
residents <- app_data$residents
assessment_data <- app_data$assessment_data
}
}
