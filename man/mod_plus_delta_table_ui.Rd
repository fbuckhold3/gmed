% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_plus_delta_table.R
\name{mod_plus_delta_table_ui}
\alias{mod_plus_delta_table_ui}
\title{Plus/Delta Table Module UI}
\usage{
mod_plus_delta_table_ui(id, title = "Plus/Delta Feedback")
}
\arguments{
\item{id}{Character string. Module namespace ID used to create unique input/output IDs.
This should be unique within your application.}

\item{title}{Character string. Optional title displayed in the card header.
Defaults to "Plus/Delta Feedback". Use NULL to hide the header entirely.}
}
\value{
A \code{tagList} containing Shiny UI elements:
- Card container with gmed styling classes
- Header section (if title provided)
- Datatable output container
- Summary statistics footer
}
\description{
Creates a user interface for displaying resident assessment plus/delta feedback
in a styled datatable. This module is designed to work with RDM 2.0 assessment
data structure and provides a consistent, professional display of evaluation
feedback filtered by resident record ID.
}
\details{
The module creates a card-based interface with:
\itemize{
\item Optional customizable title with icon
\item Styled datatable with consistent gmed theming
\item Summary statistics footer
\item Responsive design that works on mobile and desktop
}
}
\examples{
\dontrun{
# Basic usage in UI
fluidPage(
  mod_plus_delta_table_ui("resident_feedback")
)

# With custom title
mod_plus_delta_table_ui("feedback_table", title = "Assessment Results")

# Without header
mod_plus_delta_table_ui("minimal_table", title = NULL)
}

}
\seealso{
\code{\link{mod_plus_delta_table_server}} for the corresponding server function

Other plus_delta_modules: 
\code{\link{mod_plus_delta_table_server}()}
}
\concept{plus_delta_modules}
