% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/instance_counts.R
\name{count_recent_instances}
\alias{count_recent_instances}
\title{Count repeating-instrument rows for one record within a trailing window.}
\usage{
count_recent_instances(
  rdm_token,
  redcap_url,
  record_id,
  instrument,
  date_field,
  days,
  as_of = Sys.Date()
)
}
\arguments{
\item{rdm_token}{REDCap API token (test or prod — caller's choice).}

\item{redcap_url}{REDCap API base URL.}

\item{record_id}{Record to count for.}

\item{instrument}{Repeating instrument name (e.g. "assessment",
"faculty_evaluation").}

\item{date_field}{Name of the date field on that instrument to window by
(e.g. "ass_date", "fac_eval_date"). Expected as an ISO-ish date string;
parsed with \code{as.Date()}.}

\item{days}{Trailing window size in days (inclusive of today).}

\item{as_of}{Reference "today" for the window — defaults to \code{Sys.Date()},
overridable for testing/backfill scenarios.}
}
\value{
A list: \code{n} (integer count within the window), \code{dates} (the
in-window dates, for callers that want to show them), and \code{n_total}
(total rows on the instrument for this record, any date — lets a
caller distinguish "zero ever" from "zero this window").
}
\description{
Pulls only record_id/redcap_repeat_instrument/redcap_repeat_instance/
\code{date_field} for the given instrument+record (not the full form), so this
stays cheap even on instruments with many other fields.
}
