% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/instance_counts.R
\name{rc_next_instance}
\alias{rc_next_instance}
\title{Find the next available repeat-instance number for a record+instrument.}
\usage{
rc_next_instance(rdm_token, redcap_url, record_id, instrument)
}
\arguments{
\item{rdm_token}{REDCap API token (test or prod — caller's choice).}

\item{redcap_url}{REDCap API base URL.}

\item{record_id}{Record to count for.}

\item{instrument}{Repeating instrument name (e.g. "assessment",
"faculty_evaluation").}
}
\value{
Integer: 1 if the record has no existing instances of this
instrument, else max(existing instance numbers) + 1.
}
\description{
The same "pull existing instances, take max + 1" pattern used ad hoc
across ind.dash's write paths (peer review, faculty eval, questions) —
collected here once so new write paths (e.g. the resident digest's
attendance entry) don't re-derive it.
}
