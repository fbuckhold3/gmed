% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/redcap_cache.R
\name{load_cached_amion}
\alias{load_cached_amion}
\title{Load amiontools aggregate summaries from REDCap cache}
\usage{
load_cached_amion(
  rdm_token = Sys.getenv("RDM_TOKEN"),
  redcap_url = "https://redcapsurvey.slu.edu/api/",
  cache_record_id = Sys.getenv("CACHE_RECORD_ID"),
  max_age_hours = 192
)
}
\arguments{
\item{rdm_token}{REDCap API token (default: \code{RDM_TOKEN} env var).}

\item{redcap_url}{REDCap API URL.}

\item{cache_record_id}{Record ID of the cache record
(default: \code{CACHE_RECORD_ID} env var).}

\item{max_age_hours}{Warn if the cache is older than this many hours.
Set to \code{Inf} to silence the warning. Default 192h (8 days) —
the Amion cache refreshes weekly, unlike the medians cache's 3h default.}
}
\value{
Named list of data frames (same shape passed to
\code{write_amion_cache()}), or \code{NULL}.
}
\description{
Reads the aggregate Amion summaries written by \code{write_amion_cache()}.
Returns \code{NULL} silently if the cache record is unavailable or empty
so callers can fall back gracefully to a live Amion fetch.
}
